<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codingelab.MOB_final">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RemoteSearch"></activity>
        <activity android:name=".RemoteControl" />
        <activity android:name=".DataBases" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RemoteInsertActivity" />
        <activity android:name=".ListviewActivity" />
        <activity android:name=".ListviewRemoteUpdate" />
        <activity android:name=".RemoteUpdate" />
        <activity android:name=".Serverdataistview" />
        <activity android:name=".SQLitedb" />
        <activity android:name=".ResultLocal" />
        <activity android:name=".UpdateActivityLocal" />
    </application>

</manifest>